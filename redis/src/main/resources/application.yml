eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
server:
  port: 8007
spring:
  application:
    name: api-redis
logging:
    level:
      com.cloud.platform.admin.dao: DEBUG